[project]
name = "demo-agentos"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "agno==3.0.7",
  "fastapi",
  "uvicorn",
  "python-multipart",
  "sqlalchemy",
  "pyjwt",
  "openai",
  "ollama",
  "python-dotenv",
]

[tool.uv]
package = false
