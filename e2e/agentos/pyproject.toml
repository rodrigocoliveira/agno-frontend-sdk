[project]
name = "agentos-e2e"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "agno==3.0.6",
  "fastapi",
  "uvicorn",
  "python-multipart",
  "sqlalchemy",
]

[tool.uv]
package = false
